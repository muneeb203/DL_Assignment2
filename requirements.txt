# Core dependencies
numpy>=1.21.0,<2.0.0
pandas>=1.3.0
matplotlib>=3.4.0
seaborn>=0.11.0

# Machine Learning (optional - only needed for evaluation metrics in notebook)
# Note: preprocessing.py no longer requires sklearn - uses custom train_test_split
scikit-learn>=1.0.0  # Only needed for model evaluation metrics

# Deep Learning
# TensorFlow: Supports Python 3.8-3.12 (Python 3.14 not yet supported)
# If installation fails, try: pip install tensorflow-cpu or use Python 3.11
tensorflow>=2.10.0

# Jupyter
jupyter>=1.0.0
notebook>=6.4.0

